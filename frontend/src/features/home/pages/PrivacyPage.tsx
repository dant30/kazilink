import { useState } from 'react'
import { ArrowLeft, ChevronDown, ChevronUp, LockKeyhole, Phone, ShieldCheck } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'

import { Badge } from '../../../shared/components/ui/Badge'
import { Button } from '../../../shared/components/ui/Button'

const sections = [
	['collection', '1. Information We Collect'],
	['use', '2. How We Use Information'],
	['sharing', '3. When Information Is Shared'],
	['security', '4. Security and Retention'],
	['rights', '5. Your Privacy Rights'],
	['cookies', '6. Cookies and Local Storage'],
	['updates', '7. Policy Updates'],
]

const faqs = [
	['Does KaziLink publish my full National ID?', 'No. Full identity numbers, private addresses, passwords, and sensitive verification details are not displayed on public profiles.'],
	['Who can see my employment history?', 'Only authenticated users with a legitimate platform purpose and the required access can view information that you choose to share.'],
	['Can I request correction or deletion?', 'Yes. Contact KaziLink support to request access, correction, restriction, or deletion where applicable under Kenyan data-protection law.'],
]

export function PrivacyPage() {
	const navigate = useNavigate()
	const [activeSection, setActiveSection] = useState('collection')
	const [openFaq, setOpenFaq] = useState<number | null>(null)
	const goTo = (id: string) => { setActiveSection(id); document.getElementById(id)?.scrollIntoView({ behavior: 'smooth', block: 'start' }) }

	return <div className="min-h-screen bg-slate-50 px-4 py-10 sm:px-6 lg:px-8"><div className="mx-auto max-w-5xl"><div className="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div className="flex items-center gap-3"><button type="button" onClick={() => navigate('/')} className="flex items-center gap-1.5 rounded-xl border border-slate-200 bg-white px-3 py-2 text-xs font-bold text-slate-600 hover:bg-slate-100"><ArrowLeft className="h-4 w-4" />Back to marketplace</button><Badge variant="orange" size="md">Privacy document</Badge></div><Button variant="outline" size="sm" onClick={() => window.print()}>Print / Save PDF</Button></div><header className="relative mb-8 overflow-hidden rounded-3xl border border-slate-800 bg-[#0A2540] p-8 text-white shadow-xl sm:p-10"><div className="relative z-10 max-w-3xl space-y-3"><div className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-xs font-semibold"><ShieldCheck className="h-3.5 w-3.5 text-[#FF6B00]" />Kenya privacy framework</div><h1 className="text-2xl font-black tracking-tight text-white sm:text-4xl">Privacy Policy</h1><p className="text-sm leading-relaxed text-slate-300">How KaziLink collects, uses, protects, and shares information across its worker and employer marketplace.</p><div className="flex flex-wrap gap-4 pt-2 text-xs text-slate-400"><span>Last revised: September 2026</span><span>Kenya Data Protection Act 2019</span></div></div><LockKeyhole className="absolute -bottom-10 -right-8 h-64 w-64 text-white opacity-10" /></header><div className="grid grid-cols-1 gap-8 lg:grid-cols-12"><aside className="lg:col-span-4"><div className="sticky top-20 space-y-2 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><h2 className="px-2 text-xs font-bold uppercase tracking-wider text-slate-400">Quick navigation</h2><nav className="space-y-1 text-xs font-semibold">{sections.map(([id, title]) => <button type="button" key={id} onClick={() => goTo(id)} className={`w-full rounded-xl px-3 py-2.5 text-left transition ${activeSection === id ? 'border-l-4 border-[#FF6B00] bg-orange-50 font-bold text-[#FF6B00]' : 'text-slate-600 hover:bg-slate-50'}`}>{title}</button>)}</nav><div className="mt-4 border-t border-slate-100 px-2 pt-4 text-xs text-slate-500"><div className="flex items-center gap-1.5 font-bold text-slate-700"><Phone className="h-3.5 w-3.5 text-[#FF6B00]" />Privacy support</div><p className="mt-2 leading-snug">Questions about your data? Call +254 728 102 107.</p></div></div></aside><main className="space-y-6 text-sm leading-relaxed text-slate-700 lg:col-span-8">{sections.map(([id, title], index) => <section id={id} key={id} className="scroll-mt-24 space-y-4 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><div className="flex items-center gap-2"><span className={`flex h-7 w-7 items-center justify-center rounded-lg font-bold text-white ${index % 3 === 1 ? 'bg-[#FF6B00]' : 'bg-[#0A2540]'}`}>{index + 1}</span><h2 className="text-lg font-black text-slate-900">{title}</h2></div><p>{index === 0 && 'We may collect account details such as name, phone number, email, role, location, skills, availability, work history, application activity, support messages, payment references, and verification records.'}{index === 1 && 'We use information to create accounts, verify phone numbers, match workers and employers, process platform actions, prevent fraud, provide support, improve the service, and meet legal obligations.'}{index === 2 && 'Information is shared only when necessary for an authenticated marketplace action, with your consent, with service providers acting for KaziLink, or when required by law. We do not sell personal information.'}{index === 3 && 'We use access controls, authentication, audit records, and reasonable technical safeguards. Information is retained only for as long as needed for the stated purpose, legal duties, dispute handling, or platform safety.'}{index === 4 && 'Subject to applicable law, you may request access, correction, deletion, restriction, portability, or withdrawal of consent. Contact support to start a request.'}{index === 5 && 'The application may use cookies and local storage for authentication, preferences, notifications, and security. You can manage browser storage through your device settings.'}{index === 6 && 'We may update this policy when the service, law, or data practices change. The revised date will appear at the top of this page.'}</p>{index === 2 && <div className="rounded-xl border border-slate-200 bg-slate-50 p-4 text-xs"><strong>Data minimization:</strong> sensitive information is shared only for a legitimate, authenticated marketplace purpose.</div>}</section>)}<section className="space-y-4 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><div className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-[#FF6B00]" /><h2 className="text-base font-black text-slate-900">Privacy questions</h2></div>{faqs.map(([question, answer], index) => <div key={question} className="overflow-hidden rounded-xl border border-slate-200"><button type="button" onClick={() => setOpenFaq(openFaq === index ? null : index)} className="flex w-full items-center justify-between gap-3 bg-slate-50 p-4 text-left text-xs font-bold text-slate-900 hover:bg-slate-100"><span>{question}</span>{openFaq === index ? <ChevronUp className="h-4 w-4 text-[#FF6B00]" /> : <ChevronDown className="h-4 w-4 text-slate-400" />}</button>{openFaq === index && <p className="border-t border-slate-200 p-4 text-xs text-slate-600">{answer}</p>}</div>)}</section><section className="rounded-2xl bg-slate-900 p-6 text-center text-white"><h2 className="font-bold text-white">Review both documents before joining</h2><p className="mt-2 text-xs text-slate-300">Read the <Link to="/terms" className="font-bold text-orange-300 underline">Terms of Service</Link> and return to <Link to="/register" className="font-bold text-orange-300 underline">registration</Link> when ready.</p></section></main></div></div></div>
}
