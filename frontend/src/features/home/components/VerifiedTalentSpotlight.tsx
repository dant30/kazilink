import { ArrowRight, Clock3, MapPin, ShieldCheck, Star } from 'lucide-react'
import { Link } from 'react-router-dom'
import type { Job } from '../../jobs/types'

export function VerifiedTalentSpotlight({ jobs }: { jobs: Job[] }) {
  const featured = jobs.slice(0, 4)
  return <section className="bg-slate-50 py-16 sm:py-20"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><div className="inline-flex items-center gap-1.5 rounded-full bg-emerald-100 px-3 py-1 text-xs font-bold text-emerald-800"><ShieldCheck className="h-4 w-4 text-emerald-600" />Live opportunities</div><h2 className="mt-2 text-2xl font-black text-slate-900 sm:text-3xl">Work that is ready for a real person</h2><p className="mt-2 max-w-2xl text-sm text-slate-600">Browse current jobs with clear pay, location, and expectations before you apply.</p></div><Link to="/jobs" className="inline-flex items-center gap-2 font-bold text-[#FF6B00]">Browse all jobs <ArrowRight className="h-4 w-4" /></Link></div>{featured.length ? <div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{featured.map((job) => <Link key={job.id} to={`/jobs/${job.id}`} className="flex flex-col justify-between rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:border-orange-300 hover:shadow-md"><div><div className="flex items-start justify-between gap-2"><h3 className="font-bold text-slate-900">{job.title}</h3>{job.is_urgent && <span className="rounded-full bg-rose-100 px-2 py-0.5 text-[10px] font-bold text-rose-700">Urgent</span>}</div><p className="mt-2 flex items-center gap-1 text-xs text-slate-500"><MapPin className="h-3.5 w-3.5" />{job.location}</p><p className="mt-1 flex items-center gap-1 text-xs text-slate-500"><Clock3 className="h-3.5 w-3.5" />{job.job_type}</p></div><div className="mt-5 border-t border-slate-100 pt-3"><p className="text-sm font-black text-[#0A2540]">KSh {job.pay_amount_ksh.toLocaleString()} <span className="text-xs font-normal text-slate-500">/ {job.pay_period}</span></p><span className="mt-2 inline-flex items-center gap-1 text-xs font-bold text-[#FF6B00]">View role <ArrowRight className="h-3 w-3" /></span></div></Link>)}</div> : <div className="mt-8 rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center"><Star className="mx-auto h-8 w-8 text-[#FF6B00]" /><p className="mt-3 font-bold text-slate-800">New opportunities are being added.</p><p className="mt-1 text-sm text-slate-500">Create an account to be ready when the right match appears.</p></div>}</div></section>
}
